# FinTra

Plataforma de inteligência financeira pessoal — produto RhoneyInc.

Leia `CLAUDE.md` antes de mexer no projeto. Visão de produto em `PRD.md`, estado atual em `PROJECT_CONTEXT.md`, roadmap em `ROADMAP.md`. A Foundation original recebida do usuário está preservada em `docs/foundation/`.

## Stack

React + Vite + TypeScript + Tailwind CSS v4.

```bash
npm install
npm run dev
```
